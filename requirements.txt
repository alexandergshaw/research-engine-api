# Runtime deps for the Vercel @vercel/python build (lean, stateless — no DB/Redis).
# Local dev/test deps live in pyproject.toml.
Flask>=3.0
flask-smorest>=0.44
marshmallow>=3.19,<4
flask-cors>=4.0
Flask-Limiter>=3.5
Flask-Caching>=2.1
httpx>=0.27
tenacity>=8.2
pybreaker>=1.2
python-dotenv>=1.0
structlog>=24.1
